# Insurance ownership rules

Person C owns this directory. Do not edit it from the voice or clinical branches.

Use Stedi test mode only. Before implementing or calling eligibility, copy the exact approved Stedi mock patient's `firstName`, `lastName`, `dateOfBirth`, `memberId`, and `tradingPartnerServiceId` into `.env`. Never invent, normalize, or silently substitute these values. Log external request/response shapes with secrets and identity values redacted before writing a parser.
